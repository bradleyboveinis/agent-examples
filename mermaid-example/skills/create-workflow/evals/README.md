# Evals: create-workflow

Placeholder. Eval cases for the skill `create-workflow` go here.

An eval case gives the skill an input, then judges the output against a
rubric. Write the cases before the skill, and see them fail first.

Suggested layout: one `.md` file per case, holding the input, the rubric,
and the reason the case exists.
