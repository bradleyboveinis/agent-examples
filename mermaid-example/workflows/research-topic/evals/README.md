# Evals: research-topic

Placeholder. Eval cases for the workflow `research-topic` go here.

An eval case gives the workflow an input, then judges the output against a
rubric. Write the cases before the workflow, and see them fail first.

Suggested layout: one `.md` file per case, holding the input, the rubric,
and the reason the case exists.
