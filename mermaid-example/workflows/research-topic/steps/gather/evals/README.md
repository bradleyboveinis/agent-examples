# Evals: gather

Placeholder. Eval cases for the step `gather` go here.

An eval case gives the step an input, then judges the output against a
rubric. Write the cases before the step, and see them fail first.

Suggested layout: one `.md` file per case, holding the input, the rubric,
and the reason the case exists.
