# Tests: extract

Placeholder. Tests for `check-claims.mjs` go here.

This folder holds code, so it gets tests, not evals. Prompts get evals.
Code gets tests. A folder with both gets both.

Suggested layout: fixtures the script reads, plus a runner that asserts
its exit code and its output.
