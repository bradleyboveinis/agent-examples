# Evals: document-writer

Placeholder. Eval cases for the agent `document-writer` go here.

An eval case gives the agent an input, then judges the output against a
rubric. Write the cases before the agent, and see them fail first.

Suggested layout: one `.md` file per case, holding the input, the rubric,
and the reason the case exists.
